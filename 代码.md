import React, { useState } from 'react';
import { 
  Database, Search, MessageSquare, Bell, User, LogOut, Info, Settings,
  Filter, Clock, Send, Paperclip, ChevronRight, ChevronLeft, ChevronDown, FileText, 
  Folder, FolderOpen, Plus, ArrowLeft, ShieldAlert, Edit3, UploadCloud, Sparkles, Loader2, Menu, Lock,
  LayoutDashboard, Users, BookOpen, ShieldCheck, Cpu, CheckCircle, XCircle,
  // 富文本编辑器新增图标
  Bold, Italic, Underline, Heading1, Heading2, List, ListOrdered, AlignLeft, AlignCenter, AlignRight, Link, Image as ImageIcon, Quote, Code
} from 'lucide-react';

// ================= Gemini API 调用 =================
const callGeminiAPI = async (prompt, systemPrompt = "") => {
  const apiKey = ""; 
  const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=${apiKey}`;
  const payload = { contents: [{ parts: [{ text: prompt }] }] };
  if (systemPrompt) payload.systemInstruction = { parts: [{ text: systemPrompt }] };

  const delays = [1000, 2000, 4000, 8000, 16000];
  for (let attempt = 0; attempt <= 5; attempt++) {
    try {
      const response = await fetch(url, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) });
      if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
      const data = await response.json();
      return data.candidates?.[0]?.content?.parts?.[0]?.text || "抱歉，未能生成内容。";
    } catch (error) {
      if (attempt === 5) return "请求超时，请稍后重试。";
      await new Promise(res => setTimeout(res, delays[attempt]));
    }
  }
};

export default function App() {
  // ================= 认证状态与登录表单 =================
  const [currentUser, setCurrentUser] = useState(null); 
  const [loginForm, setLoginForm] = useState({ username: '', password: '' });
  const [loginError, setLoginError] = useState('');

  // ================= 侧边栏及视图状态 =================
  const [isGlobalSidebarCollapsed, setIsGlobalSidebarCollapsed] = useState(false);
  const [isRepoSidebarCollapsed, setIsRepoSidebarCollapsed] = useState(false);
  const [isSearchSidebarCollapsed, setIsSearchSidebarCollapsed] = useState(false);

  const [activeView, setActiveView] = useState('repository'); // 'repository' | 'repo-detail' | 'search' | 'qa' | 'admin'
  const [activeAdminTab, setActiveAdminTab] = useState('dashboard'); // 后台系统的子标签页
  
  const [currentRepo, setCurrentRepo] = useState(null);
  const [activeAddMenu, setActiveAddMenu] = useState(null); 
  const [isEditing, setIsEditing] = useState(false); 
  
  // ================= AI 辅助功能状态 =================
  const [aiSummary, setAiSummary] = useState('');
  const [isGeneratingSummary, setIsGeneratingSummary] = useState(false);
  const [isAiTyping, setIsAiTyping] = useState(false);
  
  // ================= 全文检索筛选模块状态 =================
  const [timeFilterType, setTimeFilterType] = useState('all');
  const [authorSearch, setAuthorSearch] = useState('');
  const [isAuthorDropdownOpen, setIsAuthorDropdownOpen] = useState(false);
  const mockAuthors = ['曹操 (研发部)', '曹丕 (市场部)', '曹植 (设计部)', '张三 (HR)', '李四 (财务)'];

  // ================= 问答模块状态 =================
  const [chatInput, setChatInput] = useState('');
  const [qaScope, setQaScope] = useState('all'); 
  const [chatHistory, setChatHistory] = useState([
    { role: 'ai', text: '你好！我是你的企业知识助手。你可以向我询问任何公司内部规定、项目资料或技术文档，我会基于内部知识库为你解答。', sources: [] }
  ]);

  // 知识库卡片数据
  const repositories = [
    { id: 'hr', title: '人力资源知识库', desc: '集中存储员工管理、招聘流程、培训资料...', bg: 'bg-blue-800', recent: [{ title: '员工绩效评估体系', time: '3 分钟前' }, { title: '薪酬福利政策', time: '5 分钟前' }] },
    { id: 'tech', title: '产品研发知识库', desc: '记录产品开发过程中的创意、设计文档...', bg: 'bg-indigo-900', recent: [{ title: '产品开发生命周期', time: '3 分钟前' }, { title: '技术创新管理', time: '5 分钟前' }] },
    { id: 'market', title: '市场运营知识库', desc: '汇总市场研究、营销策略、广告资料...', bg: 'bg-gray-200', recent: [{ title: '市场分析与定位', time: '3 分钟前' }, { title: '营销活动案例', time: '5 分钟前' }] },
    { id: 'finance', title: '财务服务知识库', desc: '包含财务报表、预算编制、税务规划...', bg: 'bg-slate-700', recent: [{ title: '财务报表分析', time: '3 分钟前' }, { title: '成本控制方法', time: '5 分钟前' }] },
    { id: 'project', title: '项目管理知识库', desc: '收集项目规划、进度跟踪、风险管理...', bg: 'bg-zinc-800', recent: [{ title: '项目规划与监控', time: '3 分钟前' }, { title: '风险评估与应对', time: '5 分钟前' }] },
    { id: 'customer', title: '客户服务知识库', desc: '提供客户咨询、服务案例、常见问题解答...', bg: 'bg-sky-700', recent: [{ title: '客户关系管理', time: '3 分钟前' }, { title: '服务流程优化', time: '5 分钟前' }] }
  ];

  // 模拟某个知识库内部的目录与笔记数据
  const [repoTreeData, setRepoTreeData] = useState([
    {
      id: 'f1', type: 'folder', name: '01_规章制度', isOpen: true,
      children: [
        { id: 'n1', type: 'note', name: '2024年员工考勤规范', author: 'HR-张三', date: '2024-01-15', level: '全员公开', content: '这是一篇关于员工考勤规范的详细笔记...\n\n1. 实习生工作时间为周一至周五，上午 9:30 至下午 18:30。\n2. 中午享有 1.5 小时休息时间。', attachments: [{name: '考勤打卡操作指南.pdf', size: '1.2MB'}, {name: '请假申请表模板.docx', size: '45KB'}] },
        { id: 'n2', type: 'note', name: '薪酬保密协议', author: 'HR-李四', date: '2023-11-02', level: '核心机密', content: '公司实行严格的薪酬保密制度...', attachments: [] }
      ]
    },
    {
      id: 'f2', type: 'folder', name: '02_新员工培训', isOpen: false,
      children: [
        { id: 'n3', type: 'file', name: '入职第一天指引.pdf', author: 'HR-王五', date: '2024-02-20', level: '全员公开', content: '[PDF文件预览区]' }
      ]
    }
  ]);

  const [selectedNode, setSelectedNode] = useState(repoTreeData[0].children[0]);

  // 处理登录
  const handleLogin = (e) => {
    e.preventDefault();
    if (loginForm.username === 'admin' && loginForm.password === '123456') {
      setCurrentUser({ username: 'admin', name: 'Admin', email: 'admin@company.com', role: 'admin', avatar: 'bg-gradient-to-tr from-blue-500 to-indigo-600' });
      setLoginError('');
      setLoginForm({ ...loginForm, password: '' });
      setActiveView('repository');
    } else if (loginForm.username === 'user' && loginForm.password === '123456') {
      setCurrentUser({ username: 'user', name: '普通员工', email: 'user@company.com', role: 'user', avatar: 'bg-gradient-to-tr from-emerald-400 to-teal-500' });
      setLoginError('');
      setLoginForm({ ...loginForm, password: '' });
      setActiveView('repository');
    } else {
      setLoginError('账号或密码错误，请重试');
    }
  };

  const toggleFolder = (folderId, nodes) => {
    return nodes.map(node => {
      if (node.id === folderId) {
        return { ...node, isOpen: !node.isOpen };
      }
      if (node.children) {
        return { ...node, children: toggleFolder(folderId, node.children) };
      }
      return node;
    });
  };

  const handleNodeClick = (node) => {
    if (node.type === 'folder') {
      setRepoTreeData(toggleFolder(node.id, repoTreeData));
    } else {
      setSelectedNode(node);
      setIsEditing(false); 
      setAiSummary(''); 
    }
  };

  const handleSendMessage = async () => {
    if (!chatInput.trim() || isAiTyping) return;
    const userMessage = chatInput;
    setChatHistory(prev => [...prev, { role: 'user', text: userMessage }]);
    setChatInput('');
    setIsAiTyping(true);
    
    const systemPrompt = `你是一个专业的企业知识管理系统(KMS) AI助手。请根据用户的问题给出专业、简洁的回答。当前用户选择的问答范围是：${qaScope === 'all' ? '全站所有知识库' : qaScope}。`;
    const aiResponse = await callGeminiAPI(userMessage, systemPrompt);
    
    const mockSources = qaScope === 'hr' || userMessage.includes('考勤') 
      ? [{ name: '2024年员工考勤规范', repoId: 'hr', nodeId: 'n1' }] 
      : [];

    setChatHistory(prev => [...prev, { role: 'ai', text: aiResponse, sources: mockSources }]);
    setIsAiTyping(false);
  };

  const generateSummary = async () => {
    if (!selectedNode || isGeneratingSummary) return;
    setIsGeneratingSummary(true);
    const prompt = `请对以下内容进行简明扼要的总结（不超过100字）：\n\n${selectedNode.content}`;
    const summary = await callGeminiAPI(prompt, "你是一个擅长提炼核心要点的AI内容摘要助手。");
    setAiSummary(summary);
    setIsGeneratingSummary(false);
  };

  const polishText = async () => {
    if (!selectedNode) return;
    const prompt = `请对以下文本进行润色，使其语气更专业、更符合企业公文规范，并修正错别字：\n\n${selectedNode.content}`;
    const polished = await callGeminiAPI(prompt, "你是一个专业的企业文案排版与润色专家。请直接返回润色后的正文内容，无需其他客套话。");
    setSelectedNode(prev => ({...prev, content: polished}));
  };

  const jumpToSource = (source) => {
    const repo = repositories.find(r => r.id === source.repoId);
    setCurrentRepo(repo);
    setActiveView('repo-detail');
  };

  const renderTree = (nodes, level = 0) => {
    return nodes.map(node => (
      <div key={node.id}>
        <div 
          className={`flex items-center justify-between py-2 px-3 cursor-pointer rounded-lg transition-colors group ${
            selectedNode?.id === node.id && node.type !== 'folder' 
              ? 'bg-blue-50 text-blue-700' 
              : 'hover:bg-gray-100 text-gray-700'
          }`}
          style={{ paddingLeft: `${level * 16 + 12}px` }}
          onClick={() => handleNodeClick(node)}
        >
          <div className="flex items-center flex-1 truncate">
            <span className="mr-2 text-gray-400 group-hover:text-gray-600 transition-colors">
              {node.type === 'folder' ? (node.isOpen ? <FolderOpen size={16}/> : <Folder size={16}/>) : 
               node.type === 'note' ? <Edit3 size={16}/> : <FileText size={16}/>}
            </span>
            <span className="text-sm truncate">{node.name}</span>
          </div>
          
          {node.type === 'folder' && (
            <div className="relative" onClick={(e) => e.stopPropagation()}>
              <button 
                onClick={() => setActiveAddMenu(activeAddMenu === node.id ? null : node.id)}
                className="hidden group-hover:flex items-center justify-center w-6 h-6 rounded hover:bg-gray-200 text-gray-500"
              >
                <Plus size={14} />
              </button>
              {activeAddMenu === node.id && (
                <div className="absolute left-full top-0 ml-1 w-28 bg-white rounded-lg shadow-lg border border-gray-100 py-1 z-30">
                  <button className="w-full text-left px-3 py-2 text-sm text-gray-700 hover:bg-blue-50 hover:text-blue-600 flex items-center" onClick={() => setActiveAddMenu(null)}>
                    <Folder size={14} className="mr-2"/> 新建目录
                  </button>
                  <button className="w-full text-left px-3 py-2 text-sm text-gray-700 hover:bg-blue-50 hover:text-blue-600 flex items-center" onClick={() => setActiveAddMenu(null)}>
                    <Edit3 size={14} className="mr-2"/> 写笔记
                  </button>
                </div>
              )}
            </div>
          )}
        </div>
        {node.type === 'folder' && node.isOpen && node.children && (
          <div className="mt-1">
            {renderTree(node.children, level + 1)}
          </div>
        )}
      </div>
    ));
  };

  // ================= 未登录：展示登录界面 =================
  if (!currentUser) {
    return (
      <div className="min-h-screen bg-[#F5F7FA] flex items-center justify-center p-4 font-sans">
        <div className="max-w-[400px] w-full bg-white rounded-2xl shadow-[0_8px_30px_rgb(0,0,0,0.04)] overflow-hidden border border-gray-100">
          <div className="bg-gradient-to-br from-blue-600 to-indigo-700 p-8 text-center">
            <div className="w-16 h-16 bg-white/20 rounded-2xl mx-auto flex items-center justify-center backdrop-blur-sm mb-4 border border-white/20 shadow-inner">
              <span className="text-white font-bold text-3xl">K</span>
            </div>
            <h2 className="text-2xl font-bold text-white tracking-tight">智库 KMS</h2>
            <p className="text-blue-100/80 text-sm mt-2 font-medium tracking-wide">企业级知识管理与协作平台</p>
          </div>
          
          <div className="p-8">
            <form onSubmit={handleLogin} className="space-y-5">
              {loginError && (
                <div className="p-3 bg-red-50 text-red-600 text-sm rounded-lg flex items-center animate-in fade-in slide-in-from-top-1">
                  <ShieldAlert size={16} className="mr-2 flex-shrink-0" />
                  {loginError}
                </div>
              )}
              
              <div>
                <label className="block text-sm font-semibold text-gray-700 mb-2">账号</label>
                <div className="relative">
                  <div className="absolute inset-y-0 left-0 pl-3.5 flex items-center pointer-events-none">
                    <User size={18} className="text-gray-400" />
                  </div>
                  <input
                    type="text"
                    className="block w-full pl-10 pr-3 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500/20 focus:border-blue-500 outline-none text-gray-700 transition-all bg-gray-50 hover:bg-white focus:bg-white text-sm"
                    placeholder="请输入登录账号"
                    value={loginForm.username}
                    onChange={e => setLoginForm({...loginForm, username: e.target.value})}
                  />
                </div>
              </div>
              
              <div>
                <label className="block text-sm font-semibold text-gray-700 mb-2">密码</label>
                <div className="relative">
                  <div className="absolute inset-y-0 left-0 pl-3.5 flex items-center pointer-events-none">
                    <Lock size={18} className="text-gray-400" />
                  </div>
                  <input
                    type="password"
                    className="block w-full pl-10 pr-3 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500/20 focus:border-blue-500 outline-none text-gray-700 transition-all bg-gray-50 hover:bg-white focus:bg-white text-sm"
                    placeholder="请输入登录密码"
                    value={loginForm.password}
                    onChange={e => setLoginForm({...loginForm, password: e.target.value})}
                  />
                </div>
              </div>
              
              <div className="pt-2">
                <button
                  type="submit"
                  className="w-full flex justify-center py-3.5 px-4 border border-transparent rounded-xl shadow-sm text-sm font-bold text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-all active:scale-[0.98]"
                >
                  登录系统
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    );
  }

  // ================= 登录成功后：展示主系统界面 =================
  return (
    <div className="flex h-screen bg-[#F5F7FA] text-gray-800 font-sans">
      
      {/* ================= 1. 全局导航侧边栏 ================= */}
      <aside 
        className={`relative bg-white border-r border-gray-200 flex flex-col justify-between shadow-sm z-30 flex-shrink-0 transition-all duration-300 ease-in-out ${
          isGlobalSidebarCollapsed ? 'w-[72px]' : 'w-64'
        }`}
      >
        <div>
          <div className={`h-16 flex items-center border-b border-gray-100 mb-4 ${isGlobalSidebarCollapsed ? 'justify-center px-0' : 'px-4'}`}>
            <button 
              onClick={() => setIsGlobalSidebarCollapsed(!isGlobalSidebarCollapsed)}
              className="p-2 text-gray-500 hover:bg-gray-100 hover:text-blue-600 rounded-lg transition-colors flex-shrink-0"
              title={isGlobalSidebarCollapsed ? "展开主菜单" : "收起主菜单"}
            >
              <Menu size={20} />
            </button>
            
            {!isGlobalSidebarCollapsed && (
              <div className="flex items-center cursor-pointer ml-2 overflow-hidden" onClick={() => setActiveView('repository')}>
                <div className="w-7 h-7 bg-blue-600 rounded flex items-center justify-center flex-shrink-0">
                  <span className="text-white font-bold text-sm">K</span>
                </div>
                <span className="text-lg font-bold text-gray-800 tracking-tight ml-2 whitespace-nowrap">智库 KMS</span>
              </div>
            )}
          </div>

          <nav className="px-3 space-y-2">
            <NavItem 
              icon={<Database size={20} />} label="知识仓库" 
              active={activeView === 'repository' || activeView === 'repo-detail'} 
              onClick={() => setActiveView('repository')} 
              collapsed={isGlobalSidebarCollapsed}
            />
            <NavItem 
              icon={<Search size={20} />} label="全文检索" 
              active={activeView === 'search'} 
              onClick={() => setActiveView('search')} 
              collapsed={isGlobalSidebarCollapsed}
            />
            <NavItem 
              icon={<MessageSquare size={20} />} label="知识问答" 
              active={activeView === 'qa'} 
              onClick={() => setActiveView('qa')} 
              collapsed={isGlobalSidebarCollapsed}
            />
          </nav>
        </div>

        <div className="p-4 border-t border-gray-100 flex flex-col space-y-2">
          <button 
            title={isGlobalSidebarCollapsed ? "通知" : ""}
            className={`flex items-center py-2 text-gray-600 hover:bg-gray-100 rounded-lg transition-colors relative ${
              isGlobalSidebarCollapsed ? 'justify-center px-0' : 'px-3'
            }`}
          >
            <Bell size={20} className={isGlobalSidebarCollapsed ? '' : 'mr-3'} />
            {!isGlobalSidebarCollapsed && <span className="text-sm font-medium truncate">通知</span>}
            <span className={`absolute bg-red-500 rounded-full ${isGlobalSidebarCollapsed ? 'top-2 right-6 w-2 h-2' : 'top-2 right-3 w-2 h-2'}`}></span>
          </button>

          <div className="relative group cursor-pointer">
            <div className={`flex items-center py-2 hover:bg-gray-100 rounded-lg transition-colors ${
              isGlobalSidebarCollapsed ? 'justify-center px-0' : 'px-3'
            }`}>
              <div className={`w-8 h-8 rounded-full ${currentUser?.avatar} text-white flex items-center justify-center font-bold shadow-sm flex-shrink-0 ${
                isGlobalSidebarCollapsed ? '' : 'mr-3'
              }`}>
                {currentUser?.name.charAt(0).toUpperCase()}
              </div>
              {!isGlobalSidebarCollapsed && (
                <div className="flex-1 overflow-hidden">
                  <p className="text-sm font-medium text-gray-800 truncate">{currentUser?.name}</p>
                </div>
              )}
            </div>

            <div className={`absolute ${
              isGlobalSidebarCollapsed ? 'left-full bottom-0 ml-2' : 'bottom-full left-0 mb-2'
            } w-56 bg-white rounded-xl shadow-lg border border-gray-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50`}>
              <div className="p-4 border-b border-gray-100">
                <p className="text-sm font-bold text-gray-800">{currentUser?.name}</p>
                <p className="text-xs text-gray-500 truncate">{currentUser?.email}</p>
              </div>
              <div className="p-2 space-y-1">
                <MenuButton icon={<User size={16}/>} label="个人中心" />
                {currentUser?.role === 'admin' && (
                  <MenuButton 
                    icon={<Settings size={16}/>} 
                    label="后台系统" 
                    onClick={() => setActiveView('admin')} 
                  />
                )}
                <MenuButton icon={<Info size={16}/>} label="关于系统" />
              </div>
              <div className="p-2 border-t border-gray-100">
                <MenuButton 
                  icon={<LogOut size={16}/>} 
                  label="退出登录" 
                  className="text-red-600 hover:bg-red-50 hover:text-red-700" 
                  onClick={() => setCurrentUser(null)}
                />
              </div>
            </div>
          </div>
        </div>
      </aside>

      {/* ================= 主内容区 ================= */}
      <main className="flex-1 flex flex-col overflow-hidden relative">
        
        {/* ================= 视图：后台管理系统 (Admin) ================= */}
        {activeView === 'admin' && (
          <div className="flex flex-col h-full bg-gray-50 animate-in fade-in duration-300">
            <div className="h-16 bg-white border-b border-gray-200 flex items-center px-8 shadow-sm flex-shrink-0 z-10 justify-between">
              <div className="flex items-center">
                <Settings className="text-indigo-600 mr-3" size={22} />
                <h1 className="text-lg font-bold text-gray-800">系统后台管理</h1>
                <span className="ml-3 px-2 py-0.5 text-xs bg-indigo-50 text-indigo-700 border border-indigo-100 rounded-md">管理控制台</span>
              </div>
              <button onClick={() => setActiveView('repository')} className="text-sm text-gray-500 hover:text-blue-600 flex items-center transition-colors">
                返回前台 <ArrowLeft size={16} className="ml-1" />
              </button>
            </div>

            <div className="flex flex-1 overflow-hidden">
              <div className="w-56 bg-white border-r border-gray-200 p-4 flex flex-col space-y-1 overflow-y-auto">
                <AdminTab active={activeAdminTab === 'dashboard'} onClick={() => setActiveAdminTab('dashboard')} icon={<LayoutDashboard size={18}/>} label="数据看板" />
                <AdminTab active={activeAdminTab === 'users'} onClick={() => setActiveAdminTab('users')} icon={<Users size={18}/>} label="组织与人员" />
                <AdminTab active={activeAdminTab === 'repos'} onClick={() => setActiveAdminTab('repos')} icon={<BookOpen size={18}/>} label="仓库管理" />
                <AdminTab active={activeAdminTab === 'audit'} onClick={() => setActiveAdminTab('audit')} icon={<ShieldCheck size={18}/>} label="内容审核" />
                <AdminTab active={activeAdminTab === 'settings'} onClick={() => setActiveAdminTab('settings')} icon={<Cpu size={18}/>} label="搜索与 AI 配置" />
              </div>

              <div className="flex-1 p-8 overflow-y-auto">
                <div className="max-w-5xl mx-auto">
                  {/* 数据看板 (简化代码结构，保留UI) */}
                  {activeAdminTab === 'dashboard' && (
                    <div className="space-y-6 animate-in fade-in">
                      <h2 className="text-xl font-bold text-gray-800 mb-4">实时运营数据</h2>
                      <div className="grid grid-cols-4 gap-6">
                        <StatCard title="全站文档总数" value="12,450" trend="+15%" trendColor="text-green-500" />
                        <StatCard title="近一周活跃用户" value="342" trend="+5%" trendColor="text-green-500" />
                        <StatCard title="AI 助手调用频次" value="8,921" trend="+22%" trendColor="text-green-500" />
                        <StatCard title="待审核文档" value="12" trend="-3%" trendColor="text-red-500" />
                      </div>
                    </div>
                  )}

                  {/* 组织与人员 */}
                  {activeAdminTab === 'users' && (
                    <div className="space-y-4 animate-in fade-in">
                      <div className="flex justify-between items-center mb-4">
                        <h2 className="text-xl font-bold text-gray-800">员工账号与权限</h2>
                        <div className="space-x-3">
                          <button className="px-4 py-2 border border-gray-300 rounded-lg text-sm text-gray-600 hover:bg-gray-50">同步企业微信/钉钉</button>
                          <button className="px-4 py-2 bg-blue-600 text-white rounded-lg text-sm hover:bg-blue-700">+ 新增账号</button>
                        </div>
                      </div>
                      <div className="bg-white border border-gray-200 rounded-xl overflow-hidden shadow-sm">
                        <table className="w-full text-left text-sm text-gray-600">
                          <thead className="bg-gray-50 border-b border-gray-200 text-gray-700">
                            <tr>
                              <th className="p-4 font-semibold">姓名 / 账号</th>
                              <th className="p-4 font-semibold">所属部门</th>
                              <th className="p-4 font-semibold">系统角色</th>
                              <th className="p-4 font-semibold">状态</th>
                              <th className="p-4 font-semibold">操作</th>
                            </tr>
                          </thead>
                          <tbody className="divide-y divide-gray-100">
                            {[
                              { name: 'Admin', acc: 'admin', dept: '信息技术部', role: '超级管理员', status: '正常' },
                              { name: '普通员工', acc: 'user', dept: '人力资源部', role: '普通员工', status: '正常' }
                            ].map((u, i) => (
                              <tr key={i} className="hover:bg-gray-50 transition-colors">
                                <td className="p-4"><p className="font-medium text-gray-800">{u.name}</p><p className="text-xs text-gray-400">{u.acc}</p></td>
                                <td className="p-4">{u.dept}</td>
                                <td className="p-4"><span className={`px-2.5 py-1 rounded-full text-xs font-medium ${u.role === '超级管理员' ? 'bg-purple-100 text-purple-700' : 'bg-gray-100 text-gray-700'}`}>{u.role}</span></td>
                                <td className="p-4"><span className={`flex items-center text-xs text-green-600`}><span className={`w-1.5 h-1.5 rounded-full mr-1.5 bg-green-500`}></span>{u.status}</span></td>
                                <td className="p-4"><button className="text-blue-600 hover:underline mr-3">编辑</button></td>
                              </tr>
                            ))}
                          </tbody>
                        </table>
                      </div>
                    </div>
                  )}
                  {/* 其他后台 Tab 简写占位 */}
                  {(activeAdminTab === 'repos' || activeAdminTab === 'audit' || activeAdminTab === 'settings') && (
                    <div className="flex items-center justify-center h-64 text-gray-400">
                      后台管理模块（请参考 v11 完整代码获取详细结构）
                    </div>
                  )}
                </div>
              </div>
            </div>
          </div>
        )}

        {/* 视图 1: 知识仓库列表 */}
        {activeView === 'repository' && (
          <div className="flex-1 overflow-auto p-8 animate-in fade-in duration-300">
            <div className="flex justify-between items-center mb-6">
              <h1 className="text-2xl font-bold text-gray-800">所有知识仓库</h1>
            </div>
            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
              {repositories.map(repo => (
                <div 
                  key={repo.id} 
                  onClick={() => { setCurrentRepo(repo); setActiveView('repo-detail'); }}
                  className="bg-white rounded-xl overflow-hidden shadow-sm border border-gray-200 hover:shadow-md hover:border-blue-300 transition-all cursor-pointer flex flex-col h-full group"
                >
                  <div className={`h-24 ${repo.bg} relative transition-transform duration-500 group-hover:scale-105 flex-shrink-0`} />
                  <div className="p-5 flex-1 flex flex-col bg-white z-10">
                    <h3 className="text-lg font-bold text-gray-800 mb-2">{repo.title}</h3>
                    <p className="text-sm text-gray-500 line-clamp-2 mb-4 flex-1">{repo.desc}</p>
                    <div className="pt-4 border-t border-gray-100">
                      <ul className="space-y-2">
                        {repo.recent?.map((item, idx) => (
                          <li key={idx} className="flex items-center justify-between text-sm group/item">
                            <div className="flex items-center text-gray-600 group-hover/item:text-blue-600 transition-colors">
                              <span className="w-1.5 h-1.5 bg-gray-300 rounded-full mr-2"></span>
                              <span className="truncate max-w-[150px]">{item.title}</span>
                            </div>
                            <span className="text-xs text-gray-400">{item.time}</span>
                          </li>
                        ))}
                      </ul>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        )}

        {/* 视图 1.5: 特定知识库详情页 */}
        {activeView === 'repo-detail' && currentRepo && (
          <div className="flex flex-1 overflow-hidden animate-in fade-in duration-300 bg-white" onClick={() => setActiveAddMenu(null)}>
            
            {/* 左侧目录树 */}
            <div className={`flex-shrink-0 bg-gray-50 border-r border-gray-200 flex flex-col transition-all duration-300 ease-in-out overflow-hidden ${
              isRepoSidebarCollapsed ? 'w-0 opacity-0 border-r-0' : 'w-[280px] opacity-100'
            }`}>
              <div className="w-[280px] flex flex-col h-full"> 
                <div className="p-4 border-b border-gray-200 flex justify-between items-center flex-shrink-0">
                  <div className="flex items-center flex-1 overflow-hidden mr-2">
                    <button onClick={() => setActiveView('repository')} className="flex flex-shrink-0 items-center text-gray-500 hover:text-blue-600 transition-colors mr-2">
                      <ArrowLeft size={16} />
                    </button>
                    <h2 className="text-sm font-bold text-gray-800 truncate" title={currentRepo.title}>{currentRepo.title}</h2>
                  </div>
                  <button 
                    onClick={() => setIsRepoSidebarCollapsed(true)} 
                    className="p-1.5 text-gray-400 hover:bg-gray-200 hover:text-gray-700 rounded-md transition-colors flex-shrink-0"
                  >
                    <ChevronLeft size={18} />
                  </button>
                </div>
                <div className="flex-1 overflow-y-auto p-2">
                  {renderTree(repoTreeData)}
                </div>
              </div>
            </div>

            {/* 右侧笔记内容 */}
            <div className="flex-1 flex flex-col overflow-y-auto relative bg-white">
              {isRepoSidebarCollapsed && (
                <div className="absolute top-5 left-5 z-10 animate-in fade-in zoom-in duration-200">
                  <button 
                    onClick={() => setIsRepoSidebarCollapsed(false)} 
                    className="px-3 py-2 bg-white border border-gray-200 shadow-sm rounded-lg text-gray-600 hover:text-blue-600 hover:bg-blue-50 transition-colors flex items-center" 
                  >
                    <Menu size={16} className="mr-2" />
                    <span className="text-sm font-medium">目录</span>
                  </button>
                </div>
              )}

              {selectedNode && selectedNode.type !== 'folder' ? (
                <div className={`max-w-4xl mx-auto w-full p-10 transition-all duration-300 ${isRepoSidebarCollapsed ? 'pt-20' : ''}`}>
                  <div className="mb-8 border-b border-gray-100 pb-6">
                    <div className="flex items-center justify-between mb-3">
                      <h1 className="text-3xl font-bold text-gray-900">{selectedNode.name}</h1>
                      <span className={`flex items-center text-xs px-2.5 py-1 rounded-full font-medium border ${
                        selectedNode.level === '核心机密' 
                          ? 'bg-red-50 text-red-700 border-red-200' 
                          : 'bg-green-50 text-green-700 border-green-200'
                      }`}>
                        {selectedNode.level === '核心机密' && <ShieldAlert size={12} className="mr-1" />}
                        {selectedNode.level}
                      </span>
                    </div>
                    
                    <div className="flex items-center text-sm text-gray-500 space-x-4 mb-4">
                      <span className="flex items-center"><User size={14} className="mr-1" /> {selectedNode.author}</span>
                      <span className="flex items-center"><Clock size={14} className="mr-1" /> {selectedNode.date}</span>
                      <span className="text-gray-300">|</span>
                      <button onClick={() => setIsEditing(!isEditing)} className={`${isEditing ? 'text-green-600 font-bold' : 'text-blue-600'} hover:underline`}>
                        {isEditing ? '取消编辑' : '编辑正文'}
                      </button>
                      <button className="text-blue-600 hover:underline">分享</button>
                    </div>

                    <div className="flex items-center space-x-3 p-3 bg-indigo-50/50 border border-indigo-100 rounded-lg">
                      <span className="text-xs font-bold text-indigo-800 flex items-center">
                        <Sparkles size={14} className="mr-1 text-indigo-500" /> AI 助手:
                      </span>
                      {!isEditing && (
                        <button 
                          onClick={generateSummary}
                          disabled={isGeneratingSummary}
                          className="flex items-center text-xs font-medium px-3 py-1.5 bg-white border border-indigo-200 text-indigo-600 rounded-md hover:bg-indigo-50 transition-colors disabled:opacity-50"
                        >
                          {isGeneratingSummary ? <Loader2 size={12} className="mr-1 animate-spin" /> : <Sparkles size={12} className="mr-1" />}
                          {isGeneratingSummary ? '摘要生成中...' : '✨ 智能摘要'}
                        </button>
                      )}
                      {isEditing && (
                        <button 
                          onClick={polishText}
                          className="flex items-center text-xs font-medium px-3 py-1.5 bg-white border border-indigo-200 text-indigo-600 rounded-md hover:bg-indigo-50 transition-colors"
                        >
                          ✨ 文本排版优化
                        </button>
                      )}
                    </div>
                    
                    {aiSummary && !isEditing && (
                      <div className="mt-4 p-4 bg-gradient-to-r from-blue-50 to-indigo-50 border-l-4 border-indigo-500 rounded-r-lg relative animate-in fade-in slide-in-from-top-2">
                        <button onClick={() => setAiSummary('')} className="absolute top-2 right-2 text-indigo-400 hover:text-indigo-600">×</button>
                        <h4 className="text-xs font-bold text-indigo-800 mb-1 flex items-center"><Sparkles size={12} className="mr-1" /> AI 摘要</h4>
                        <p className="text-sm text-indigo-900 leading-relaxed">{aiSummary}</p>
                      </div>
                    )}
                  </div>

                  {/* ================= 富文本编辑器 / 阅读区 ================= */}
                  {isEditing ? (
                    <div className="border border-gray-300 rounded-xl overflow-hidden focus-within:ring-2 focus-within:ring-blue-100 focus-within:border-blue-400 transition-all bg-white shadow-sm">
                      {/* 富文本工具栏 */}
                      <div className="bg-gray-50 border-b border-gray-200 p-2 flex flex-wrap items-center gap-1 select-none">
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="加粗 (Ctrl+B)"><Bold size={16} /></button>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="斜体 (Ctrl+I)"><Italic size={16} /></button>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="下划线 (Ctrl+U)"><Underline size={16} /></button>
                        <div className="w-px h-5 bg-gray-300 mx-1"></div>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="一级标题"><Heading1 size={16} /></button>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="二级标题"><Heading2 size={16} /></button>
                        <div className="w-px h-5 bg-gray-300 mx-1"></div>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="无序列表"><List size={16} /></button>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="有序列表"><ListOrdered size={16} /></button>
                        <div className="w-px h-5 bg-gray-300 mx-1"></div>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="左对齐"><AlignLeft size={16} /></button>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="居中对齐"><AlignCenter size={16} /></button>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="右对齐"><AlignRight size={16} /></button>
                        <div className="w-px h-5 bg-gray-300 mx-1"></div>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="插入链接"><Link size={16} /></button>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="插入图片"><ImageIcon size={16} /></button>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="引用块"><Quote size={16} /></button>
                        <button className="p-1.5 text-gray-600 hover:bg-gray-200 hover:text-gray-900 rounded transition-colors" title="代码块"><Code size={16} /></button>
                      </div>
                      
                      {/* 编辑内容区 */}
                      <textarea 
                        className="w-full min-h-[400px] p-5 bg-transparent outline-none resize-y text-gray-700 leading-relaxed font-sans text-[15px]"
                        value={selectedNode.content}
                        onChange={(e) => setSelectedNode({...selectedNode, content: e.target.value})}
                        placeholder="在此输入或粘贴富文本内容..."
                      />
                      
                      {/* 底部保存按钮 (内部) */}
                      <div className="bg-gray-50 border-t border-gray-200 p-3 flex justify-end">
                        <button 
                          onClick={() => setIsEditing(false)}
                          className="px-5 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 transition-colors shadow-sm"
                        >
                          保存笔记内容
                        </button>
                      </div>
                    </div>
                  ) : (
                    <div className="prose prose-blue max-w-none text-gray-700 leading-relaxed whitespace-pre-line">
                      {selectedNode.content}
                    </div>
                  )}
                  {/* ===================================================== */}

                  {(isEditing || (selectedNode.attachments && selectedNode.attachments.length > 0)) && (
                    <div className="mt-10 pt-6 border-t border-gray-100">
                      <h3 className="text-sm font-bold text-gray-800 mb-4 flex items-center">
                        <Paperclip size={16} className="mr-2 text-gray-500" /> 
                        笔记附件 {selectedNode.attachments ? `(${selectedNode.attachments.length})` : '(0)'}
                      </h3>
                      
                      {!isEditing && selectedNode.attachments && selectedNode.attachments.length > 0 && (
                        <div className="flex flex-wrap gap-4">
                          {selectedNode.attachments.map((file, idx) => (
                            <div key={idx} className="flex items-center px-4 py-3 border border-gray-200 rounded-lg hover:border-blue-400 hover:bg-blue-50 cursor-pointer bg-gray-50 transition-colors w-64">
                              <FileText size={20} className="text-blue-500 mr-3 flex-shrink-0" />
                              <div className="overflow-hidden">
                                <p className="text-sm font-medium text-gray-700 truncate" title={file.name}>{file.name}</p>
                                <p className="text-xs text-gray-400 mt-0.5">{file.size}</p>
                              </div>
                            </div>
                          ))}
                        </div>
                      )}

                      {isEditing && (
                        <button className="w-full py-6 border-2 border-dashed border-gray-200 rounded-xl flex flex-col items-center justify-center text-gray-400 hover:bg-gray-50 hover:border-blue-300 hover:text-blue-500 transition-colors">
                          <UploadCloud size={24} className="mb-2" />
                          <span className="text-sm font-medium">点击或将文件拖拽到此处上传</span>
                          <span className="text-xs mt-1 opacity-70">支持上传 Word、PDF 作为补充资料</span>
                        </button>
                      )}
                    </div>
                  )}
                </div>
              ) : (
                <div className="flex-1 flex items-center justify-center text-gray-400">
                  <div className="text-center">
                    <FileText size={48} className="mx-auto mb-4 opacity-20" />
                    <p>请在左侧选择要查看的笔记或文档</p>
                  </div>
                </div>
              )}
            </div>
          </div>
        )}

        {/* 视图 2: 全文检索 */}
        {activeView === 'search' && (
          <div className="flex flex-col h-full animate-in fade-in duration-300">
            <div className="h-20 bg-white border-b border-gray-200 flex items-center px-8 shadow-sm z-10 flex-shrink-0">
              <div className="relative w-full max-w-3xl flex items-center">
                <Search className="absolute left-4 text-gray-400" size={20} />
                <input 
                  type="text" 
                  placeholder="搜索全站文档、笔记、表格内容..." 
                  className="w-full h-12 pl-12 pr-4 bg-gray-50 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:bg-white transition-all text-base"
                />
                <button className="absolute right-2 bg-blue-600 text-white px-4 py-1.5 rounded-md text-sm font-medium hover:bg-blue-700">
                  搜索
                </button>
              </div>
            </div>
            
            <div className="flex flex-1 overflow-hidden">
              <div className={`flex-shrink-0 bg-white border-r border-gray-200 flex flex-col transition-all duration-300 ease-in-out overflow-hidden ${
                isSearchSidebarCollapsed ? 'w-0 opacity-0 border-r-0' : 'w-[280px] opacity-100'
              }`}>
                <div className="w-[280px] h-full p-6 overflow-y-auto">
                  <div className="flex items-center justify-between text-sm font-bold text-gray-800 mb-6 pb-4 border-b border-gray-100">
                    <div className="flex items-center"><Filter size={16} className="mr-2" /> 筛选条件</div>
                    <button 
                      onClick={() => setIsSearchSidebarCollapsed(true)} 
                      className="p-1.5 text-gray-400 hover:bg-gray-100 hover:text-gray-700 rounded-md transition-colors"
                    >
                      <ChevronLeft size={18} />
                    </button>
                  </div>
                  
                  <div className="mb-6">
                    <h3 className="text-sm font-semibold text-gray-700 mb-2">所属组织 / 知识库</h3>
                    <select className="w-full bg-gray-50 border border-gray-300 text-gray-700 text-sm rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 block p-2.5 outline-none cursor-pointer hover:bg-white transition-colors">
                      <option>全部组织</option>
                      <option>人力资源部</option>
                      <option>产品研发部</option>
                      <option>市场运营部</option>
                    </select>
                  </div>

                  <div className="mb-6">
                    <h3 className="text-sm font-semibold text-gray-700 mb-2">文件类型</h3>
                    <select className="w-full bg-gray-50 border border-gray-300 text-gray-700 text-sm rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 block p-2.5 outline-none cursor-pointer hover:bg-white transition-colors">
                      <option>全部格式</option>
                      <option>PDF 文档</option>
                      <option>Word (.docx)</option>
                      <option>在线笔记</option>
                    </select>
                  </div>

                  <div className="mb-6">
                    <h3 className="text-sm font-semibold text-gray-700 mb-2">发布时间</h3>
                    <select 
                      className="w-full bg-gray-50 border border-gray-300 text-gray-700 text-sm rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 block p-2.5 outline-none cursor-pointer hover:bg-white transition-colors"
                      value={timeFilterType}
                      onChange={(e) => setTimeFilterType(e.target.value)}
                    >
                      <option value="all">不限时间</option>
                      <option value="1d">近一天</option>
                      <option value="1w">近一周</option>
                      <option value="1m">近一月</option>
                      <option value="1y">近一年</option>
                      <option value="custom">自定义范围...</option>
                    </select>
                    
                    {timeFilterType === 'custom' && (
                      <div className="flex flex-col space-y-2 mt-3 p-3 bg-gray-50 rounded-lg border border-gray-200 animate-in slide-in-from-top-2 duration-200">
                        <div>
                          <span className="text-xs text-gray-500 mb-1 block">起始日期</span>
                          <input type="date" className="w-full text-sm p-2 border border-gray-300 rounded outline-none focus:border-blue-500 focus:ring-1 focus:ring-blue-500 bg-white" />
                        </div>
                        <div>
                          <span className="text-xs text-gray-500 mb-1 block">结束日期</span>
                          <input type="date" className="w-full text-sm p-2 border border-gray-300 rounded outline-none focus:border-blue-500 focus:ring-1 focus:ring-blue-500 bg-white" />
                        </div>
                      </div>
                    )}
                  </div>

                  <div className="mb-6">
                    <h3 className="text-sm font-semibold text-gray-700 mb-2">发布人员</h3>
                    <div className="relative">
                      <div className="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                        <User size={14} className="text-gray-400" />
                      </div>
                      <input 
                        type="text" 
                        placeholder="输入姓名 (如: 曹)" 
                        className="w-full bg-gray-50 border border-gray-300 text-gray-700 text-sm rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 block pl-9 p-2.5 outline-none hover:bg-white transition-colors"
                        value={authorSearch}
                        onChange={(e) => {
                          setAuthorSearch(e.target.value);
                          setIsAuthorDropdownOpen(true);
                        }}
                        onFocus={() => setIsAuthorDropdownOpen(true)}
                        onBlur={() => setTimeout(() => setIsAuthorDropdownOpen(false), 200)} 
                      />
                      
                      {isAuthorDropdownOpen && authorSearch && (
                        <div className="absolute z-20 w-full mt-1 bg-white border border-gray-200 rounded-lg shadow-xl max-h-48 overflow-y-auto animate-in fade-in zoom-in-95 duration-100">
                          {mockAuthors.filter(a => a.includes(authorSearch)).length > 0 ? (
                            mockAuthors.filter(a => a.includes(authorSearch)).map((author, idx) => (
                              <div 
                                key={idx}
                                className="px-3 py-2.5 text-sm text-gray-700 hover:bg-blue-50 hover:text-blue-700 cursor-pointer flex items-center transition-colors"
                                onClick={() => {
                                  setAuthorSearch(author);
                                  setIsAuthorDropdownOpen(false);
                                }}
                              >
                                <div className="w-5 h-5 rounded bg-blue-100 text-blue-600 flex items-center justify-center mr-2 text-xs font-bold">
                                  {author.charAt(0)}
                                </div>
                                <span>
                                  {author.split(authorSearch).map((part, i, arr) => 
                                    <React.Fragment key={i}>
                                      {part}{i !== arr.length - 1 && <span className="text-blue-600 font-bold">{authorSearch}</span>}
                                    </React.Fragment>
                                  )}
                                </span>
                              </div>
                            ))
                          ) : (
                            <div className="px-4 py-3 text-sm text-gray-400 text-center">无匹配人员</div>
                          )}
                        </div>
                      )}
                    </div>
                  </div>
                </div>
              </div>

              <div className="flex-1 bg-gray-50 p-8 flex items-center justify-center overflow-y-auto relative">
                {isSearchSidebarCollapsed && (
                  <div className="absolute top-5 left-5 z-10 animate-in fade-in zoom-in duration-200">
                    <button 
                      onClick={() => setIsSearchSidebarCollapsed(false)} 
                      className="px-3 py-2 bg-white border border-gray-200 shadow-sm rounded-lg text-gray-600 hover:text-blue-600 hover:bg-blue-50 transition-colors flex items-center" 
                    >
                      <Filter size={16} className="mr-2" />
                      <span className="text-sm font-medium">筛选</span>
                    </button>
                  </div>
                )}

                <div className="text-center text-gray-400">
                  <Search size={48} className="mx-auto mb-4 opacity-20" />
                  <p>输入关键词开始检索，支持拼音、同义词</p>
                </div>
              </div>
            </div>
          </div>
        )}

        {/* 视图 3: 知识问答 */}
        {activeView === 'qa' && (
          <div className="flex flex-col h-full bg-white animate-in fade-in duration-300">
            <div className="h-14 border-b border-gray-200 flex items-center px-6 justify-between bg-gray-50 shadow-sm z-10 flex-shrink-0">
              <div className="flex items-center text-sm font-medium text-gray-700">
                <span className="mr-3">🎯 提问范围：</span>
                <select 
                  className="bg-white border border-gray-300 rounded-md px-3 py-1.5 outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 min-w-[150px] cursor-pointer"
                  value={qaScope}
                  onChange={(e) => setQaScope(e.target.value)}
                >
                  <option value="all">全站所有知识库</option>
                  <option value="hr">人力资源知识库</option>
                  <option value="tech">产品研发知识库</option>
                  <option value="market">市场运营知识库</option>
                </select>
              </div>
              <p className="text-xs text-gray-400">缩小范围可显著提高回答准确性</p>
            </div>

            <div className="flex-1 overflow-y-auto p-6 space-y-6">
              <div className="max-w-4xl mx-auto space-y-6">
                {chatHistory.map((msg, index) => (
                  <div key={index} className={`flex ${msg.role === 'user' ? 'justify-end' : 'justify-start'}`}>
                    <div className="flex max-w-[80%]">
                      {msg.role === 'ai' && (
                        <div className="w-8 h-8 rounded bg-blue-100 flex items-center justify-center mr-3 flex-shrink-0 mt-1">
                          <span className="text-blue-600 font-bold text-sm">AI</span>
                        </div>
                      )}
                      
                      <div className={`p-4 rounded-2xl ${msg.role === 'user' ? 'bg-blue-600 text-white rounded-tr-sm' : 'bg-gray-50 border border-gray-100 text-gray-800 rounded-tl-sm'}`}>
                        <p className="leading-relaxed whitespace-pre-line">{msg.text}</p>
                        
                        {msg.sources && msg.sources.length > 0 && (
                          <div className="mt-3 pt-3 border-t border-gray-200">
                            <p className="text-xs text-gray-500 mb-2">📑 参考来源 (点击跳转原文)：</p>
                            <div className="flex flex-wrap gap-2">
                              {msg.sources.map((src, i) => (
                                <button 
                                  key={i} 
                                  onClick={() => jumpToSource(src)}
                                  className="inline-flex items-center px-2 py-1 bg-white border border-blue-200 rounded text-xs text-blue-600 cursor-pointer hover:bg-blue-50 hover:border-blue-400 transition-colors shadow-sm"
                                >
                                  <FileText size={12} className="mr-1" /> {src.name}
                                </button>
                              ))}
                            </div>
                          </div>
                        )}
                      </div>
                    </div>
                  </div>
                ))}

                {isAiTyping && (
                  <div className="flex justify-start">
                    <div className="flex max-w-[80%]">
                      <div className="w-8 h-8 rounded bg-blue-100 flex items-center justify-center mr-3 flex-shrink-0 mt-1">
                        <span className="text-blue-600 font-bold text-sm">AI</span>
                      </div>
                      <div className="p-4 rounded-2xl bg-gray-50 border border-gray-100 text-gray-800 rounded-tl-sm flex items-center space-x-2">
                        <div className="w-2 h-2 bg-blue-400 rounded-full animate-bounce" style={{ animationDelay: '0ms' }}></div>
                        <div className="w-2 h-2 bg-blue-400 rounded-full animate-bounce" style={{ animationDelay: '150ms' }}></div>
                        <div className="w-2 h-2 bg-blue-400 rounded-full animate-bounce" style={{ animationDelay: '300ms' }}></div>
                      </div>
                    </div>
                  </div>
                )}
              </div>
            </div>

            <div className="p-6 bg-white border-t border-gray-100 flex-shrink-0">
              <div className="max-w-4xl mx-auto relative border border-gray-300 rounded-2xl shadow-sm focus-within:ring-2 focus-within:ring-blue-100 focus-within:border-blue-500 transition-all p-2">
                <textarea 
                  className="w-full max-h-32 min-h-[60px] p-2 bg-transparent outline-none resize-none text-gray-700"
                  placeholder={`基于 "${qaScope === 'all' ? '所有知识库' : repositories.find(r=>r.id===qaScope)?.title}" 提问... (按回车发送)`}
                  value={chatInput}
                  onChange={(e) => setChatInput(e.target.value)}
                  disabled={isAiTyping}
                  onKeyDown={(e) => {
                    if (e.key === 'Enter' && !e.shiftKey) {
                      e.preventDefault();
                      handleSendMessage();
                    }
                  }}
                />
                <div className="flex justify-end px-2 pt-2">
                  <button 
                    onClick={handleSendMessage}
                    disabled={isAiTyping || !chatInput.trim()}
                    className={`px-4 py-1.5 rounded-lg flex items-center text-sm font-medium transition-colors ${chatInput.trim() && !isAiTyping ? 'bg-blue-600 text-white hover:bg-blue-700' : 'bg-gray-100 text-gray-400'}`}
                  >
                    {isAiTyping ? <Loader2 size={16} className="mr-2 animate-spin"/> : <Send size={16} className="mr-2"/>}
                    发送
                  </button>
                </div>
              </div>
            </div>
          </div>
        )}
      </main>
    </div>
  );
}

// 辅助 UI 组件
function NavItem({ icon, label, active, onClick, collapsed }) {
  return (
    <button 
      onClick={onClick}
      title={collapsed ? label : ""}
      className={`w-full flex items-center py-3 rounded-lg transition-all ${
        collapsed ? 'justify-center px-0' : 'px-3'
      } ${
        active 
          ? 'bg-blue-50 text-blue-700 font-semibold' 
          : 'text-gray-600 hover:bg-gray-100 font-medium'
      }`}
    >
      <div className={`${active ? 'text-blue-600' : 'text-gray-500'} ${collapsed ? '' : 'mr-3'}`}>
        {icon}
      </div>
      {!collapsed && <span className="truncate">{label}</span>}
    </button>
  );
}

function MenuButton({ icon, label, className = "text-gray-700 hover:bg-gray-50", onClick }) {
  return (
    <button onClick={onClick} className={`w-full flex items-center px-3 py-2 text-sm rounded-md transition-colors ${className}`}>
      <span className="mr-3 opacity-70">{icon}</span>
      {label}
    </button>
  );
}

function AdminTab({ icon, label, active, onClick }) {
  return (
    <button 
      onClick={onClick} 
      className={`w-full flex items-center px-4 py-3 text-sm font-medium rounded-lg transition-colors ${
        active ? 'bg-indigo-50 text-indigo-700' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'
      }`}
    >
      <span className={`mr-3 ${active ? 'text-indigo-600' : 'text-gray-400'}`}>{icon}</span>
      {label}
    </button>
  );
}

function StatCard({ title, value, trend, trendColor }) {
  return (
    <div className="bg-white p-5 rounded-xl border border-gray-200 shadow-sm flex flex-col justify-between">
      <h3 className="text-sm font-medium text-gray-500">{title}</h3>
      <div className="mt-2 flex items-baseline justify-between">
        <span className="text-2xl font-bold text-gray-800">{value}</span>
        <span className={`text-xs font-bold bg-gray-50 px-2 py-1 rounded-md ${trendColor}`}>{trend}</span>
      </div>
    </div>
  );
}