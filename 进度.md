# KMS MVP 进度看板

## 当前状态

- 项目目标：可上线 MVP
- 当前轮次：Iteration 002
- 当前重点：先完成主链路功能，再逐步补齐页面高保真还原
- 当前运行方式：
  - 后端使用现有容器镜像 + 挂载源码 + `uvicorn`
  - 前端使用现有容器镜像 + 挂载源码 + standalone 启动
  - 这样做的原因是外部镜像代理偶发 `500`，暂时不适合依赖标准 `docker compose build frontend` 重建链路

## 已确定方案

- 前端：Next.js
- 后端：FastAPI
- 数据库：MySQL
- 对象存储：MinIO
- 检索：Elasticsearch
- 权限模型：管理员分配角色与密级，低级员工不能搜索或查看高级信息
- 笔记存储：`TipTap JSON + content_text`
- 审核方向：规则 + 敏感词

## 当前判断（2026-04-06）

### 这个项目要做什么
- 这是一个企业知识管理系统（KMS）MVP。
- 核心目标是把企业知识按“知识仓库 -> 目录 -> 笔记 -> 附件”沉淀下来，并提供权限控制、全文检索、问答和后台管理。
- 目标交付内容包括：
  - 用户登录与权限控制
  - 知识仓库浏览、笔记阅读与编辑
  - PDF / DOCX 附件入库
  - Elasticsearch 全文检索
  - 基于权限过滤的 RAG 问答
  - 管理后台与基础部署能力

### 当前代码实际状态
- 项目不是空壳，主链路已经具备可演示的 MVP 骨架。
- 后端已实现 JWT 登录、当前用户、仓库/笔记读取、笔记编辑、附件上传、搜索、问答、后台 CRUD 等接口。
- 前端已实现仓库、仓库详情、笔记详情、笔记编辑、搜索、问答、后台等页面，并接上真实后端数据。
- 当前仍处于“开发态可联调”阶段，不等同于“正式用户态可上线”阶段。

### 本轮更新（2026-04-06）
- 登录循环的根因是 Next.js `<Link>` 预加载；已把侧边导航与品牌区的跳转改为原生 `<a>`，登录后跳转不再丢失会话。
- 真实登录 + 会话 cookie 模式已接入并能正常跳转仓库页面。
- 引入 Alembic，补了初始化迁移，并在启动时自动执行迁移或必要时自动 `stamp head` 以兼容已有表。
- 后台补充了用户/角色管理接口与前端表单，可创建/编辑/删除用户、分配角色与密级、启停账号。
- 搜索索引升级为 chunk 级：每个笔记按内容+附件文本切分后单独建 ES 文档，查询结果去重保留最佳命中，高亮更聚焦。
- 前端依赖已安装并完成 `npm run build`。
- 附件支持预览/下载/删除/替换：后端增补接口，编辑页可管理，详情页提供预览+下载入口。
- PowerShell 读取 UTF-8 中文文件的注意事项已写入 `任务.md`，避免因终端编码误判源码。
- 附件下载链路已改为前端代理路由，真实登录下可携带授权头访问受保护资源。
- 前端构建已完成；仍未做浏览器全量回归。

### 本轮更新（2026-04-07，认证与鉴权加固）
- CORS 已改为运行时白名单机制，默认从环境变量加载，并可由后台在线更新后即时生效。
- 后台新增安全配置与审计读取能力：
  - `GET/PUT /api/v1/admin/security/cors-origins`
  - `GET /api/v1/admin/security/auth-audit`
- 安全配置权限已收紧为 `platform_admin`（仓库管理员不再可改全局安全策略）。
- 生产环境启动保护已落地：
  - `SECRET_KEY=change-me` 时拒绝启动；
  - CORS 仍是 localhost 默认白名单时拒绝启动；
  - 生产环境空白名单拒绝启动。
- Token/Cookie TTL 已统一为 12 小时：
  - 后端 `ACCESS_TOKEN_EXPIRE_MINUTES=720`
  - 前端会话 Cookie `maxAge=12h`
- 登录失败次数限制已落地：
  - 连续密码错误 5 次后锁定账号（默认 5 分钟）
  - 进入锁定后每再输错 1 次，锁定时长额外 +5 分钟
  - 登录接口返回剩余次数/锁定时间，登录页展示明确反馈文案
- 已实现 `jti + token_version` 双撤销机制：
  - JWT 新增 `jti`、`ver`
  - 鉴权时校验 `token_version`
  - 新增 `revoked_tokens` 表，支持按 `jti` 精确吊销
  - 新增 `POST /api/v1/auth/logout`，退出时吊销当前 token 的 `jti`
- 前端 401 清理会话链路已闭环：
  - 页面鉴权与 API 请求遇 401 统一跳 `/logout`
  - `/logout` 先调后端注销，再清理本地 cookie
- 已新增认证审计模型与日志记录，覆盖登录/退出/鉴权失败/鉴权拒绝等关键事件。
- 修复 Alembic 在 MySQL 8.4 的兼容问题（`TEXT` 默认值非法），并将 `0002` 迁移改为幂等，避免“半迁移状态”导致重复字段报错。
- 新增专项文档 `docs/安全基线.md`，用于按模块沉淀“当前必须保持”的安全决策并约束后续改动流程。

### 当前明确存在的问题
- 认证鉴权主链路已加固，但尚未做一轮完整浏览器回归（覆盖登录锁定、401 自动清会话、后台安全配置）。
- 问答仍是“检索后整理回答”的 MVP，不是真正的大模型生成。
- Celery 与异步入库链路目前只有骨架，占位多于真实执行。

### 编码与排查说明
- 当前开发环境使用 Windows PowerShell 5.1，默认读取 UTF-8 中文文件时可能出现乱码显示。
- 这类乱码优先视为终端读取编码问题，而不是源码一定损坏。
- 在终端查看中文代码或文档时，应显式使用 `Get-Content -Encoding UTF8`，避免误判。

## 已完成

### 文档与项目骨架
- 重写了 [任务.md](D:/program/code/kms/任务.md)
- 建立了前后端目录与基础工程
- 建立了 [docker-compose.yml](D:/program/code/kms/docker-compose.yml)、[.env](D:/program/code/kms/.env)、[.env.example](D:/program/code/kms/.env.example)
- 建立了持续更新的 [进度.md](D:/program/code/kms/进度.md)

### 后端基础能力
- 完成数据库初始化与种子数据
- 完成 JWT 登录接口：`POST /api/v1/auth/login`
- 完成当前用户接口：`GET /api/v1/auth/me`
- 完成仓库列表接口：`GET /api/v1/repositories`
- 完成仓库详情接口：`GET /api/v1/repositories/{slug}`
- 完成笔记详情接口：`GET /api/v1/repositories/{slug}/notes/{noteId}`
- 完成笔记更新接口：`PUT /api/v1/repositories/{slug}/notes/{noteId}`
- 已接入多级权限过滤，低密级用户无法读取高密级仓库或笔记

### 前端主页面
- 完成仓库列表页
- 完成仓库详情页
- 完成笔记详情页
- 完成笔记编辑页
- 完成搜索页
- 完成问答页
- 完成后台页基础骨架
- 完成登录页基础骨架

### 编辑器与内容能力
- 编辑页已从开发态 `textarea` 升级为 TipTap
- 前后端已支持保存 `content_json + content_text`
- 笔记更新后会同步刷新 Elasticsearch 索引

### 附件能力
- 已接入 MinIO
- 已完成笔记附件上传接口
- 已完成附件下载跳转接口
- 已限制附件类型为 `pdf` / `docx`
- 已限制单文件 20MB
- 笔记详情页已展示附件列表
- 编辑页已支持上传附件
- 已完成一次真实上传验证，测试笔记 `hr/notes/1` 当前带有 `kms-upload-check.pdf`
- 已新增附件正文抽取能力，支持对新上传的 `pdf` / `docx` 解析文本
- 已将附件正文并入 Elasticsearch 索引
- 新上传附件的正文现在可以参与搜索与问答

### 搜索能力
- 已建立 `kms_notes` Elasticsearch 索引
- 服务启动时会自动同步现有笔记到索引
- 笔记更新、附件上传后会自动刷新索引
- 已完成搜索接口：`GET /api/v1/search?q=...`
- 已完成权限过滤后的搜索结果页
- 已验证管理员搜索“考勤”可命中 `hr/notes/1`
- 已验证普通员工搜索“RAG”返回空结果，权限过滤生效
- 已验证搜索关键词 `KMSATTACHMENTCHECK` 可命中新上传 docx 的附件正文

### 问答能力
- 已完成问答接口：`GET /api/v1/qa?q=...`
- 当前问答为“权限过滤后检索，再整理回答”的 MVP 链路
- 问答结果已返回来源列表
- 前端 `/qa` 页面已从静态占位改为真实问答页
- 已支持按仓库范围提问
- 已支持从问答结果跳转到对应笔记详情页
- 已验证管理员提问“考勤”可返回 1 条来源
- 已验证普通员工提问“RAG”返回 0 条来源
- 已验证问答关键词 `KMSATTACHMENTCHECK` 可引用新上传 docx 的附件正文

### 后台 CRUD
- 已完成后台内容总览接口：`GET /api/v1/admin/content`
- 已完成仓库 CRUD：
  - `POST /api/v1/admin/repositories`
  - `PUT /api/v1/admin/repositories/{id}`
  - `DELETE /api/v1/admin/repositories/{id}`
- 已完成目录 CRUD：
  - `POST /api/v1/admin/folders`
  - `PUT /api/v1/admin/folders/{id}`
  - `DELETE /api/v1/admin/folders/{id}`
- 已完成笔记 CRUD：
  - `POST /api/v1/admin/notes`
  - `PUT /api/v1/admin/notes/{id}`
  - `DELETE /api/v1/admin/notes/{id}`
- 已限制后台管理接口仅 `platform_admin` / `repo_admin` 可用
- 已在删除仓库、删除目录、删除笔记后同步刷新或清理 Elasticsearch 索引
- 前端 `/admin` 页面已接入真实后台管理表单，支持创建、更新、删除与跳转编辑器

### 页面与样式
- 已修复前端 standalone 运行方式下静态 CSS `404`
- 已恢复样式正常加载
- 已把共享壳层和核心页面逐步往 [代码.md](D:/program/code/kms/代码.md) 的视觉方向靠近
- 目前页面比最初工程版明显更接近原型，但还不是高保真复刻

### 已识别但待继续处理的问题
- 真实登录代码路径已补上，但还需要浏览器联调验证登录、登出、仓库跳转和附件下载是否稳定。
- 当前搜索筛选 UI 仍有占位项，尚未真正映射到后端查询参数。
- 当前问答仍是检索式回答，尚未接入 Gemini 生成链路。
- 当前 Celery 任务仍是占位实现，未真正承担异步入库流程。
- 当前 Celery 任务仍是占位实现，未真正承担异步入库流程。

## 已验证

- `python -m compileall backend/app`
- 前端容器内 `npm run build`
- 页面 [http://localhost:3000/repositories](http://localhost:3000/repositories)
- 页面 [http://localhost:3000/repositories/hr](http://localhost:3000/repositories/hr)
- 页面 [http://localhost:3000/repositories/hr/notes/1](http://localhost:3000/repositories/hr/notes/1)
- 页面 [http://localhost:3000/repositories/hr/notes/1/edit](http://localhost:3000/repositories/hr/notes/1/edit)
- 页面 [http://localhost:3000/search](http://localhost:3000/search)
- 页面 [http://localhost:3000/qa](http://localhost:3000/qa)
- 页面 [http://localhost:3000/qa?q=考勤](http://localhost:3000/qa?q=%E8%80%83%E5%8B%A4)
- 页面 [http://localhost:3000/admin](http://localhost:3000/admin)
- 后端健康检查 [http://localhost:8000/api/v1/health](http://localhost:8000/api/v1/health)
- 管理员访问 `GET /api/v1/admin/content`
- 普通员工访问 `GET /api/v1/admin/content` 返回 `403`
- 已完成一轮“创建仓库 -> 创建目录 -> 创建笔记 -> 更新笔记 -> 删除仓库”的串行 CRUD 验证，并在验证后清理测试数据
- 已完成一轮“创建临时仓库 -> 创建临时笔记 -> 上传带正文的 docx -> 搜索命中附件正文 -> 问答引用附件正文 -> 删除临时仓库”的串行验证，并在验证后清理测试数据

## 当前未完成

- 认证鉴权加固后的完整浏览器回归尚未完成（尤其是账号锁定、401 自动登出、后台安全配置保存）
- 当前问答仍是检索式整理回答，尚未接入真正的大模型生成
- 当前页面仍不是 [代码.md](D:/program/code/kms/代码.md) 的高保真复刻
- 容器化启动链路仍需要在你当前 Docker Desktop 环境下再做一次端到端验收

## 当前约束与说明

- 当前仓库是从空目录逐步搭建出来的，不是基于现成工程增量改造
- 前端认证链路正在从开发态直通切换到真实登录会话，当前需要补浏览器联调确认
- 数据库初始化已改用 Alembic 迁移，空库自动 `upgrade head`，已有表自动 `stamp head`。
- 终端里查看中文返回时可能出现编码显示异常，排查时应优先使用 `Get-Content -Encoding UTF8`
- 容器化联调尚未验证（本机 Docker npipe 权限异常）

## 建议测试路径

1. 登录与仓库浏览
- 打开 [http://localhost:3000/repositories](http://localhost:3000/repositories)
- 进入人力资源知识库
- 打开笔记详情与编辑页

2. 附件能力
- 在编辑页上传 PDF 或 DOCX
- 返回笔记详情页确认附件是否出现

3. 搜索能力
- 打开 [http://localhost:3000/search?q=考勤](http://localhost:3000/search?q=%E8%80%83%E5%8B%A4)
- 确认能看到真实结果和跳转入口

4. 问答能力
- 打开 [http://localhost:3000/qa?q=考勤](http://localhost:3000/qa?q=%E8%80%83%E5%8B%A4)
- 确认能看到回答、来源数量与来源跳转

## 下一步

1. 完成认证与鉴权加固后的浏览器回归（锁定、注销、401 清会话、后台 CORS 白名单）
2. 升级搜索筛选和问答生成能力
3. 推进 Celery 异步入库从骨架到可执行链路
